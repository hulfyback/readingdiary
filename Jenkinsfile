pipeline {
  agent any

  stages {
    stage('Compile Stage') {
      steps {
        dir("reading_diary") {
          sh "ls -la ${pwd()}"       
        }
        sh "ls -la ${pwd()}"
      }
    }
  }
}